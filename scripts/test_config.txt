self.Q_hover = np.diag([
    150, 150, 300,          # 位置 (x,y,z) - 保持
    10, 10, 200,    # 速度 (vx,vy,vz) - 保持
    170.0, 170.0, 20.0,     # 姿态 (roll/pitch/yaw)：从150→100（降低超调），仍高于初始80
    10.0, 10.0, 5.0,    # 角速度 (p,q,r)：从8e3→5e3（降低阻尼过强），仍高于初始4e3
    20.0, 20.0, 2.0,          # 位置误差积分 - 保持
    0.0, 0.0, 0.0           # 姿态误差积分：从0.4→1.0（提升积分阻尼，抑制积分累积震荡）
])
self.P_hover = self.Q_hover * 0.8  # 终端权重略低，保证终端收敛温和
# 控制量权重：[Fx,Fy,Fz,τx,τy,τz] - 避免控制量饱和9
# self.R_hover = np.diag([
#     0.5, 0.5, 5.0,        # Fx/Fy（水平推力）权重高（限制过载），Fz（主推力）权重低（允许调整）
#     55.0, 55.0, 60.0        # τx/τy（滚转/俯仰力矩）权重高，τz（偏航力矩）权重低
# ])
# # 控制率权重：平滑控制输入，避免倾转机构突变
# self.S_hover = np.diag([
#     5, 5, 5.0,          # Fx/Fy控制率权重高，Fz控制率权重低
#     200.0, 200.0, 30.0        # 力矩控制率 - 匹配倾转机构动态特性
# ])